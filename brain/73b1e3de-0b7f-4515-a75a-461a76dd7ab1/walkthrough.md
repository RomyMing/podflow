# Walkthrough: PRD 分阶段计划对齐 SAD

## 变更概要

将 PRD (`PROJECT_PRD.md`) 的分阶段计划从产品侧的 "一期/二期/三期" 改为与 SAD 一致的 **Phase 0-4 技术交付里程碑体系**，确保产品文档与技术架构文档的阶段划分完全统一。

## 修改内容

### 1. 文档头信息更新
- 版本号: `v1.0` → `v1.1`
- 新增 `最后更新: 2026-04-06`
- 关联文档链接：将不可移植的本地路径改为仓库内的 SAD 和 ADS 相对链接。

### 2. 第 1.2 节 — 阶段性目标 (行 30-65)

| 修改前 | 修改后 |
|--------|--------|
| 一期目标 - MVP 验证 | **Phase 0** - 项目初始化 ✅ 已完成 |
| 二期目标 - 体验优化 | **Phase 1** - MVP 验证（单/双人播客） |
| 三期目标 - 商业化 | **Phase 2** - 体验优化 |
| (缺失) | **Phase 3** - 生产强化 |
| (缺失) | **Phase 4** - 商业化与多语种 |

新增了技术基础设施搭建（Phase 0）和生产强化（Phase 3）两个阶段，这两个阶段在 SAD 中存在但 PRD 中完全缺失。

### 3. 第 3.2 节 — Kano 表格优先级 (行 129-150)

- 列名: `MVP 一期` → `阶段`
- 所有"✅"标记改为 `✅ Phase 1`
- "❌ 二期" → `❌ Phase 2`
- "❌ 三期" → `❌ Phase 4`（对应商业化阶段）

### 4. 第七节 — 完全重写 (行 399-498)

**修改前**: "MVP 一期范围总结"（仅包含 7.1 做什么 + 7.2 不做什么）

**修改后**: "分阶段交付路线"，包含：
- **7.0 Phase 0** — 项目初始化状态表（已完成清单）
- **7.1 Phase 1** — MVP 交付路线（含 Mermaid Gantt 图 + 功能范围表）
- **7.2 Phase 2** — 体验优化功能表（含工期估算）
- **7.3 Phase 3** — 生产强化功能表（含工期估算）
- **7.4 Phase 4** — 商业化与多语种功能表

Phase 1 的 Gantt 图与 SAD 5.3 节对齐，新增了"用户体系"section，明确了手机号+微信登录（14d）和配额管理（7d）的排期。

### 5. 附录链接修正

`详见架构设计方案` → `详见 [架构设计方案（SAD）](../../PROJECT_SAD.md)`

## 未处理的事项

以下代码问题已在 [implementation_plan.md](implementation_plan.md) 中记录，等待 Phase 1 用户体系搭建时修复：

- `security.py` — 缺 Refresh Token、decode 无异常处理、硬编码、缺 Docstring
- `User` 模型 — 缺 `updated_at`/`is_active`/`quota_reset_at` 字段
- 受保护文件（`config.py`/`base.py`）的问题需 Architect 审批

## 变更文件

render_diffs(../../PROJECT_PRD.md)
